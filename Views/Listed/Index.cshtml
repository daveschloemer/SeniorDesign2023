@using FreshBooks.Data.ViewModels;
@model ListedTabVM

@{
    ViewData["Title"] = "Your Listed Books for Sell";
}
<div class="row">
    <div class="col-md-8 offset-2">
        <div>
            <h4>List of Book For Selll</h4>
        </div>

        <table class="table">
            <thead>
                <tr>
                    <th>Selected Amount</th>
                    <th>Books</th>
                    <th>Price</th>
                    <th>Subtotal</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.ListedTab.ListedTabItems)
                {
                }
            </tbody>
        </table>

    </div>
</div>